parser:

	- remove quotes and flag it in t_parser
	- remove double quotessand flag it in t_parser
	- replace all $XXXX with real value

	- if test='a  b  c' bash get only one space max
	- if test='a  b      c' and echo "$test" its output all space (doesn't skip them)
	- si c'est entre quotes c'est uniquement du test pas d'assignation


bash: export: `a-b=salut': not a valid identifier
export peut commencer par un underscore

parsing:

	- double quotes
	- replace all $ par les env (a-Z,0-9,_)
	- split a chaque si c'est pas escaper (backslash escape, quotes aussi et dquotes)

ROADMAP ->
	tokenize all quoted or double quoted string skipping \
	split by ;
	replace all env vars
	split by space and format them
	exec
